[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — do not force, so Netlify Forms POST handling takes precedence
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
